name: mung_ge_mung_ge
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+3
  path: ^1.8.0
  cupertino_icons: ^1.0.2
  geolocator: ^7.0.1
  google_maps_flutter: ^2.0.3
  intl: ^0.17.0
  firebase_auth: ^1.0.1
  google_sign_in: ^5.0.1
  sign_in_with_apple: ^3.0.0
  firebase_core: ^1.0.2

  flutter_form_builder: ^6.0.0-nullsafety.1
  provider: ^5.0.0

  shared_preferences: ^2.0.5

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  http: ^0.13.1
  flutter_html: ^2.0.0-nullsafety.0
  infinite_scroll_pagination: ^3.0.1
  url_launcher: ^6.0.3
  email_validator: ^2.0.1
  crypto: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/auth/
    - assets/database/localdb.sqlite
